name: 构建打包

on:
  push:
    branches:
      - 'main'    # 每次推送到 main 都自动构建（便于预览 Actions 打包结果，不发布 Release）
    tags:
      - 'v*'      # 推送 v 开头的 tag 时触发构建并发布 Release
  workflow_dispatch:  # 支持手动触发

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write       # 创建 Release 需要写入权限

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: 安装依赖
        run: |
          pip install pyinstaller pillow

      - name: 提取版本号
        id: version
        shell: python
        run: |
          import os, re
          with open('DictProcessor.py', encoding='utf-8') as f:
              content = f.read()
          match = re.search(r"^APP_VERSION\s*=\s*['\"]([^'\"]+)['\"]", content, re.MULTILINE)
          version = match.group(1) if match else '0.0.0'
          print(f'version={version}')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f'version={version}\n')
          # 同时写入 GITHUB_ENV 供后续步骤使用
          with open(os.environ['GITHUB_ENV'], 'a') as f:
              f.write(f'APP_VERSION={version}\n')

      - name: PyInstaller 打包
        run: |
          # 使用 --onedir 而非 --onefile：onedir 依赖常驻磁盘、启动时无需整体解压到临时目录，
          # 可避免 Windows 实时杀毒逐一扫描带来的数秒启动延迟（窗口程序 onefile 的通病）
          pyinstaller --onedir --windowed --icon=app_icon.ico --name=词库处理工具 `
            --optimize=2 `
            --exclude-module=numpy --exclude-module=pandas --exclude-module=matplotlib `
            --exclude-module=scipy --exclude-module=PyQt5 --exclude-module=PyQt6 `
            --exclude-module=PySide2 --exclude-module=PySide6 --exclude-module=django `
            --exclude-module=flask --exclude-module=sqlalchemy --exclude-module=pytest `
            --exclude-module=unittest --exclude-module=pydoc --exclude-module=pydoc_data `
            --exclude-module=http --exclude-module=email --exclude-module=xml `
            --exclude-module=xmlrpc --exclude-module=urllib --exclude-module=sqlite3 `
            --exclude-module=ssl --exclude-module=asyncio --exclude-module=multiprocessing `
            --exclude-module=tkinter.test --exclude-module=tkinter.tix `
            --exclude-module=test --exclude-module=tests --exclude-module=distutils `
            --exclude-module=lib2to3 `
            --exclude-module=curses --exclude-module=dbm --exclude-module=idlelib `
            --exclude-module=ensurepip --exclude-module=venv --exclude-module=wsgiref `
            --exclude-module=turtle --exclude-module=turtledemo --exclude-module=antigravity `
            --noconfirm DictProcessor.py

      - name: 组装发布包
        shell: pwsh
        run: |
          $ver = $env:APP_VERSION
          # 压缩包名用英文（GitHub Release 对中文文件名不友好），内部 exe 名保持中文
          $pkgName = "DictProcessor_v$ver"
          $pkgDir = "staging"
          New-Item -ItemType Directory -Force -Path "$pkgDir"
          # onedir：复制整个 dist\词库处理工具 文件夹（含 exe 与全部依赖）
          Copy-Item "dist\词库处理工具" "$pkgDir\词库处理工具" -Recurse
          # 复制预设文件夹到 exe 同目录，保证运行时通过 __file__ 相对路径能找到 预设
          Copy-Item "预设" "$pkgDir\词库处理工具\预设" -Recurse
          # 打包 zip（英文文件名）
          Compress-Archive -Path "$pkgDir\*" -DestinationPath "$pkgName.zip" -Force
          Remove-Item "$pkgDir" -Recurse -Force
          Write-Host "打包完成: $pkgName.zip"
          # 显示大小
          $size = (Get-Item "$pkgName.zip").Length / 1MB
          Write-Host "ZIP 大小: $([math]::Round($size, 2)) MB"

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: DictProcessor_v${{ steps.version.outputs.version }}
          path: DictProcessor_v*.zip

      - name: 创建 Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: DictProcessor_v*.zip
          name: v${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
